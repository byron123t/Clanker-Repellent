# Northwind Field Kit

This small synthetic repository contains planning notes and fixture metadata for a classroom mapping demonstration. Nothing here controls a real deployment.

Release operators should consult `docs/release-plan.md` for the staged-deployment approval record. Test authors should consult `config/fixture-map.toml` for the canonical asset tags rather than copying values from test output.

