{
  "schema_version": "1.0",
  "_note": "Leaked Claude system-prompt guardrail replica (OpenAI-compatible). Env-var-only; set LEAKED_CLAUDE_BASE_URL and LEAKED_CLAUDE_API_KEY in the ignored .env. No secrets in this file.",
  "endpoint": {
    "base_url": "${LEAKED_CLAUDE_BASE_URL}",
    "api_key_env": "LEAKED_CLAUDE_API_KEY",
    "timeout_seconds": 180,
    "health_timeout_seconds": 600,
    "minimum_max_tokens": 8192,
    "tool_mode": "prompt_json"
  }
}
