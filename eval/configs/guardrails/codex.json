{
  "schema_version": "1.0",
  "_note": "Codex / OpenAI-compatible coding endpoint. Env-var-only; set CODEX_BASE_URL and CODEX_API_KEY in the ignored .env. No secrets in this file.",
  "endpoint": {
    "base_url": "${CODEX_BASE_URL}",
    "api_key_env": "CODEX_API_KEY",
    "timeout_seconds": 180,
    "health_timeout_seconds": 600,
    "minimum_max_tokens": 8192,
    "tool_mode": "prompt_json"
  }
}
